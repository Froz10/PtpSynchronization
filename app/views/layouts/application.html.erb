<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>PtpSynchronization</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <%= favicon_link_tag asset_path("ptp-sync.png")%>
  </head>

  <body>
    <%= render 'shared/header' %>
    <div class="flex">
      <%= render 'shared/sidebar' if current_user %>
      <div class="flex-1 w-full">
        <% if notice %>
          <div
            data-controller="error"
            data-error-target="elementToHide"
            class="flex justify-between text-green-700 border border-green-400 shadow-inner rounded relative p-3 bg-green-100" role="alert">
            <p class="notice"><%= notice %></p>
              <strong data-action="click->error#toggle" class="text-xl lighn-center cursor-pointer">
                &times;
              </strong>
          </div>
        <% end %>
        <% if alert %>
          <div
            data-controller="error"
            data-error-target="elementToHide"
            class="flex justify-between text-red-700 border border-red-400 shadow-inner rounded relative p-3 bg-red-100" role="alert">
            <p class="alert"><%= alert %></p>
              <strong data-action="click->error#toggle" class="text-xl lighn-center cursor-pointer">
                &times;
              </strong>
          </div>
        <% end %>
        
        <%= yield %>
      </div>
  </div>  
  </body>
</html>
